export const articleGet = (id: number) => {
    return 'bar' + id
}

/*
export const {pending, data: post, error} = (id: number) => {
    useLazyFetch(`https://www.marche.be/api/post.php?id=${id}`)
}*/